.next/
node_modules/
tsconfig.tsbuildinfo
.env
.env.*
!.env.example
.env*.local
.venv/
__pycache__/
.pytest_cache/
.claude/settings.local.json
.mcp.json

# Stale backup copy of snajp-support (from Downloads, 2026-07-22) — not source
snajp-support/00-LASMIG.md
snajp-support/backend/
snajp-support/frontend/
snajp-support/databas/

# Local scratch files (not project source)
auth_err.txt
auth_out.txt
new_auth_err.txt
new_auth_out.txt
*.zip
*.zip.lnk
supabase/.db-password.txt
.vercel/
dist/
coverage/
.agent-chorus/
marketingskills.zip

.vercel

.snipra-auth-state.json
screenshots/cookies-dump.json

# Verification screenshots — 73 MB of session evidence, not source. They stay on
# disk and the plans reference them by path; nothing builds or runs from them.
.shots/
.vercel

# Design-hook telemetry: per-turn session reports and the ledger. Machine
# state, regenerated every run, never read from git. Untracked 2026-08-01;
# files stay on disk.
.impeccable/

# Materialized per-tenant context packs (snajp-support/app/leads/context_pack.py)
# — runtime state derived from agent_context_docs, regenerated every run.
snajp-support/var/
